--- xgboost
+++ inaccel-xgboost
@@ -102,6 +102,9 @@
   find_package(OpenMP REQUIRED)
 endif (USE_OPENMP)

+# inaccel-coral
+list(APPEND LINKED_LIBRARIES_PRIVATE ${PROJECT_SOURCE_DIR}/inaccel/libcoral-api.a)
+
 # dmlc-core
 msvc_use_static_runtime()
 add_subdirectory(${xgboost_SOURCE_DIR}/dmlc-core)
